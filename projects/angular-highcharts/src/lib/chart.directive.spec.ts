import { TestBed, waitForAsync } from '@angular/core/testing';

import { ChartDirective } from './chart.directive';

describe('AngularHighchartsComponent', () => {
  it('stub true', function() {
    expect(true).toEqual(true);
  });
});
